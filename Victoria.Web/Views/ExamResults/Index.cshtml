@model List<Victoria.Web.Models.Education.ExamResultListViewModel>
@{
    ViewData["Title"] = "Exam Results";
}

<h2 class="mb-3">Exam Results</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<div class="mb-3">
    <a class="btn btn-primary" href="/ExamResults/Create">+ Add result</a>
</div>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Id</th>
            <th>SessionId</th>
            <th>StudentId</th>
            <th>Score</th>
            <th>Created</th>
            <th style="width:120px;"></th>
        </tr>
    </thead>
    <tbody>
        @if (Model == null || !Model.Any())
        {
            <tr><td colspan="6" class="text-center">No results.</td></tr>
        }
        else
        {
            foreach (var x in Model)
            {
                <tr>
                    <td>@x.Id</td>
                    <td>@x.ExamSessionId</td>
                    <td>@x.StudentId</td>
                    <td>@x.Score</td>
                    <td>@x.CreatedAt.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>
                        <a class="btn btn-sm btn-primary" href="/ExamResults/Details/@x.Id">Details</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
