@model Victoria.Web.Models.Documents.DocumentUploadVisaViewModel

@{
    ViewData["Title"] = "Upload Visa Document";
}

<h2 class="mb-3">Upload Visa Document</h2>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">@ViewBag.Error</div>
}

<form asp-action="UploadVisa" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()

    <input type="hidden" asp-for="VisaApplicationId" />

    <div class="mb-3">
        <label class="form-label">Document type</label>

        <select class="form-select" asp-for="Title">
            <option value="">-- select --</option>

            <option value="Passport">Passport</option>
            <option value="Acceptance letter">Acceptance letter</option>
            <option value="Visa application form">Visa application form</option>
            <option value="Proof of accommodation">Proof of accommodation</option>
            <option value="Travel insurance">Travel insurance</option>
            <option value="Bank statements / proof of funds">Bank statements / proof of funds</option>
        </select>


        <span class="text-danger" asp-validation-for="Title"></span>
        <div class="form-text">Must match checklist DocumentType exactly.</div>
    </div>


    <div class="mb-3">
        <label class="form-label">Description (optional)</label>
        <input class="form-control" asp-for="Description" />
        <span class="text-danger" asp-validation-for="Description"></span>
    </div>

    <div class="mb-3">
        <label class="form-label">File</label>
        <input class="form-control" asp-for="File" type="file" />
        <span class="text-danger" asp-validation-for="File"></span>
    </div>

    <button class="btn btn-success" type="submit">Upload</button>
    <a class="btn btn-secondary" href="/Documents/Visa?visaApplicationId=@Model.VisaApplicationId">Cancel</a>
</form>
