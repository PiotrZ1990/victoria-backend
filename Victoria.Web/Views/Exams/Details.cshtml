@model Victoria.Web.Models.Education.ExamDetailsViewModel
@{
    ViewData["Title"] = "Exam Details";
}

<h2 class="mb-3">Exam Details</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<div class="card">
    <div class="card-body">
        <p><strong>Id:</strong> @Model.Id</p>
        <p><strong>Name:</strong> @Model.Name</p>
        <p><strong>Type:</strong> @Model.ExamType</p>
        <p><strong>Active:</strong> @(Model.IsActive ? "Yes" : "No")</p>
        <p><strong>Created:</strong> @Model.CreatedAt.ToString("yyyy-MM-dd HH:mm")</p>

        @if (!string.IsNullOrWhiteSpace(Model.Description))
        {
            <hr />
            <p>@Model.Description</p>
        }

        <hr />
        <a class="btn btn-secondary" href="/Exams">← Back</a>
        <a class="btn btn-warning ms-2" href="/Exams/Edit/@Model.Id">Edit</a>

        <form method="post" action="/Exams/Delete/@Model.Id" style="display:inline;">
            @Html.AntiForgeryToken()
            <button class="btn btn-danger ms-2" onclick="return confirm('Delete?');">Delete</button>
        </form>
    </div>
</div>
